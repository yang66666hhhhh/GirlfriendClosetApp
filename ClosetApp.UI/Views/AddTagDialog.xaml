<Window x:Class="ClosetApp.UI.Views.AddTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加标签" Width="350" Height="250"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Background="{DynamicResource BackgroundBrush}" Margin="24">
        <StackPanel>
            <TextBlock Text="标签名称" FontSize="14" Margin="0,0,0,8"/>
            <TextBox x:Name="TxtName" Padding="12" Margin="0,0,0,16"/>

            <TextBlock Text="颜色" FontSize="14" Margin="0,0,0,8"/>
            <ComboBox x:Name="CmbColor" Margin="0,0,0,24">
                <ComboBoxItem Content="紫色" Tag="#667eea"/>
                <ComboBoxItem Content="粉色" Tag="#ec4899"/>
                <ComboBoxItem Content="蓝色" Tag="#3b82f6"/>
                <ComboBoxItem Content="绿色" Tag="#10b981"/>
                <ComboBoxItem Content="橙色" Tag="#f59e0b"/>
                <ComboBoxItem Content="红色" Tag="#ef4444"/>
            </ComboBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="取消" Padding="16" Click="Cancel_Click"/>
                <Button Grid.Column="2" Content="添加" Padding="16" Click="Save_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>