<Window x:Class="ClosetApp.UI.Views.DeleteConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认删除" Width="350" Height="200"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Background="{DynamicResource BackgroundBrush}" Margin="24">
        <StackPanel>
            <TextBlock x:Name="TxtMessage" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,24"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="取消" Padding="16" Click="Cancel_Click"/>
                <Button Grid.Column="2" Content="删除" Padding="16" Background="#ef4444" Foreground="White" Click="Confirm_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>