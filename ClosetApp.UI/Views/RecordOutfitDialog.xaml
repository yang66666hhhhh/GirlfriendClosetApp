<Window x:Class="ClosetApp.UI.Views.RecordOutfitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="记录穿搭" Width="400" Height="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Background="{DynamicResource BackgroundBrush}" Margin="24">
        <StackPanel>
            <TextBlock Text="选择今日搭配" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
            <ItemsControl x:Name="OutfitsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="OutfitBorder" Padding="16" Margin="0,0,0,8" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Cursor="Hand" MouseLeftButtonDown="OutfitBorder_Click" BorderBrush="Transparent" BorderThickness="2">
                            <TextBlock Text="{Binding Name}" FontSize="14"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="取消" Padding="16" Click="Cancel_Click"/>
                <Button Grid.Column="2" Content="确认" Padding="16" Click="Confirm_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>