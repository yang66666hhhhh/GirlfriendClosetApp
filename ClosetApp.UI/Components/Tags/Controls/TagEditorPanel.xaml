<UserControl x:Class="ClosetApp.UI.Components.Tags.Controls.TagEditorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#FFE8E2DC"/>
        <SolidColorBrush x:Key="InputFocusBrush" Color="#FFD9A299"/>

        <Style x:Key="NameInput" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="{StaticResource Font.Primary}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" BorderBrush="{StaticResource InputBorderBrush}"
                                BorderThickness="1" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource InputFocusBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CategoryRadio" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bg" Background="Transparent"
                                BorderBrush="{DynamicResource BorderLightBrush}"
                                BorderThickness="1" CornerRadius="20"
                                Padding="16,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource PrimaryLightBrush}"/>
                                <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource SurfaceModalBrush}"
            MinWidth="420" MinHeight="320" MaxHeight="520"
            Width="420" CornerRadius="20"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="6" Opacity="0.10" Color="#40000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" BorderBrush="{DynamicResource BorderLightBrush}"
                    BorderThickness="0,0,0,1" Margin="20,0">
                <Grid>
                    <TextBlock x:Name="TxtTitle" Text="新建标签"
                               FontFamily="{StaticResource Font.Primary}"
                               FontSize="17" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <Button HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Style="{StaticResource ModalCloseButton}"
                            Click="Close_Click"/>
                </Grid>
            </Border>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,16,20,0">
                <StackPanel>
                    <!-- 标签名称 -->
                    <TextBlock Text="标签名称"
                               FontFamily="{StaticResource Font.Primary}"
                               FontSize="12" FontWeight="Medium"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtName" Style="{StaticResource NameInput}"
                             Tag="输入标签名称" Margin="0,0,0,20"
                             TextChanged="TxtName_TextChanged"/>

                    <!-- 标签分类 -->
                    <TextBlock Text="标签分类"
                               FontFamily="{StaticResource Font.Primary}"
                               FontSize="12" FontWeight="Medium"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,0,8"/>
                    <WrapPanel Margin="0,0,0,20">
                        <RadioButton x:Name="RbStyle" Content="风格" GroupName="TagCategory"
                                     Style="{StaticResource CategoryRadio}" IsChecked="True"/>
                        <RadioButton x:Name="RbScene" Content="场景" GroupName="TagCategory"
                                     Style="{StaticResource CategoryRadio}"/>
                        <RadioButton x:Name="RbSeason" Content="季节" GroupName="TagCategory"
                                     Style="{StaticResource CategoryRadio}"/>
                    </WrapPanel>

                    <!-- 标签颜色 -->
                    <TextBlock Text="标签颜色"
                               FontFamily="{StaticResource Font.Primary}"
                               FontSize="12" FontWeight="Medium"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,0,10"/>
                    <WrapPanel Margin="0,0,0,20">
                        <RadioButton x:Name="Color1" GroupName="TagColor" Tag="#D8B7A3"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#D8B7A3"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color2" GroupName="TagColor" Tag="#C8C2B8"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#C8C2B8"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color3" GroupName="TagColor" Tag="#B7C4B2"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#B7C4B2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color4" GroupName="TagColor" Tag="#E88D8D"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#E88D8D"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color5" GroupName="TagColor" Tag="#D4A5C9"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#D4A5C9"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color6" GroupName="TagColor" Tag="#8BA8D9"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#8BA8D9"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color7" GroupName="TagColor" Tag="#A8D4A8"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#A8D4A8"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                        <RadioButton x:Name="Color8" GroupName="TagColor" Tag="#C89B7B"
                                     Style="{x:Null}" Cursor="Hand" Margin="0,0,10,8"
                                     Click="Color_Click">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="Bg" Width="32" Height="32" CornerRadius="16"
                                            BorderThickness="2" BorderBrush="Transparent">
                                        <Ellipse Fill="#C89B7B"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                    </WrapPanel>

                    <!-- 预览 -->
                    <TextBlock Text="预览"
                               FontFamily="{StaticResource Font.Primary}"
                               FontSize="12" FontWeight="Medium"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,0,8"/>
                    <Border x:Name="PreviewChip" HorizontalAlignment="Left"
                            Background="#FFF0EBE6" CornerRadius="20" Padding="14,7">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse Width="10" Height="10" Fill="#D8B7A3"/>
                            <TextBlock x:Name="TxtPreview" Text="标签名"
                                       FontFamily="{StaticResource Font.Primary}"
                                       FontSize="13"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}"
                    BorderThickness="0,1,0,0" Margin="20,0,20,0">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1"
                            Content="取消"
                            Style="{StaticResource ModalCancelButton}"
                            Margin="0,0,10,0"
                            Click="Cancel_Click"/>
                    <Button Grid.Column="2"
                            Content="保存"
                            Style="{StaticResource ModalSaveButton}"
                            Click="Save_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
