<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ModalFooterButtonBase" TargetType="Button" BasedOn="{StaticResource AppButtonBase}">
        <Setter Property="MinHeight" Value="44"/>
        <Setter Property="MinWidth" Value="104"/>
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="FontSize" Value="{DynamicResource Button.FontSize.Medium}"/>
    </Style>

    <Style x:Key="ModalCancelButton" TargetType="Button" BasedOn="{StaticResource ModalFooterButtonBase}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="ModalSaveButton" TargetType="Button" BasedOn="{StaticResource ModalFooterButtonBase}">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Effect" Value="{StaticResource Shadow.Button}"/>
        <Setter Property="MinWidth" Value="152"/>
    </Style>

    <Style x:Key="ModalDangerButton" TargetType="Button" BasedOn="{StaticResource ModalFooterButtonBase}">
        <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Effect" Value="{StaticResource Shadow.Button}"/>
    </Style>

</ResourceDictionary>
